<?php

$versao = '?v=0.7';

echo $this
        ->headScript()
        ->prependFile($this->basePath() . '/js/circuito_da_visao/modal-selecionar-perfil.js' . $versao)
        ->prependFile($this->basePath() . '/js/circuito_da_visao/page-login.js' . $versao)
;
echo $this->inlineScript();