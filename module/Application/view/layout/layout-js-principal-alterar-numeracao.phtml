<?php

$versao = '?v=0.1';

echo $this
        ->headScript()
        ->prependFile($this->basePath() . '/js/circuito_da_visao/validar-principal-alterar-numeracao.js' . $versao);
echo $this->inlineScript();
